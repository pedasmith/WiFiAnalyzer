<UserControl
    x:Class="SpeedTests.SpeedTestControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SpeedTests"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="700"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <ScrollViewer Height="500">
                <TextBlock x:Name="uiLog" IsTextSelectionEnabled="true" TextWrapping="Wrap" />
            </ScrollViewer>
            <StackPanel Orientation="Horizontal">
                <Button Content="Test!" Click="OnTest" />
            </StackPanel>
        </StackPanel>
        <StackPanel Grid.Column="1" Orientation="Horizontal">
            <StackPanel x:Name="ZZZuiLatencyGraphPanel" Orientation="Horizontal" VerticalAlignment="Top">
            </StackPanel>
        </StackPanel>
        <ListBox Grid.Column="1" x:Name="uiLatencyGraphPanel"
                 HorizontalAlignment="Left" VerticalAlignment="Top" VerticalContentAlignment="Top"
                 ScrollViewer.HorizontalScrollBarVisibility="Visible" 
                 SelectionChanged="OnSelectChange" Height="220"
                 >
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <VirtualizingStackPanel Orientation="Horizontal"  VerticalAlignment="Top" />
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
        </ListBox>
        <local:StatisticsControl Grid.Column="2" x:Name="uiLatencyStats" VerticalAlignment="Top" />
    </Grid>
</UserControl>
